# LogScope Configuration Example
# Copy these values to .env in the root directory and customize as needed

# Backend Server Configuration
PORT=3000

# Frontend Server Configuration
VITE_PORT=5173
VITE_API_URL=http://localhost:3000

# Backend Storage Configuration
LOG_DIR=./logs
MAX_INDEX_SIZE=10000

# Auto-Cleanup Configuration
## How often to run cleanup checks (in milliseconds). Default: 10000 (10 seconds)
CLEANUP_INTERVAL_MS=10000

## Maximum combined logs before cleanup triggers (default: 500)
LOG_MAX_TOTAL=500

## Age threshold: logs older than this are deleted (in milliseconds). Default: 3600000 (1 hour)
LOG_MAX_AGE_MS=3600000

## How many logs to delete per capacity-based cleanup. Default: 100
LOG_DELETE_COUNT=100

# Node.js Environment
NODE_ENV=development
LOG_LEVEL=info
